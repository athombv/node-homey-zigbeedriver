name: ESLint

on: [pull_request]

jobs:
  eslint:
    name: Run
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm install --only=dev && echo $NODE_ENV && ./node_modules/.bin/eslint .
