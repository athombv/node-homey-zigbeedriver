name: ESLint

on: [pull_request]

jobs:
  eslint:
    name: Run
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo "$NODE_ENV" && npm install --only=dev && ./node_modules/.bin/eslint .
